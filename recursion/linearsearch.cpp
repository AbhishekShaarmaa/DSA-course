#include<iostream>
using namespace std;
bool linearsearch(int *arr;int size; int key)
{
    // base case
    if(size==arr[0])
    {
        return true ;
    }
    // cheking for key
     if (key == arr[i])
       {
        return true ;
       }
       else 
       {
        bool remiainingpart  = linearsearch(arr+1,size-1);
        return remaining part;  
       }
    
}

int main()
{
    int n=5;
    int arr[5]= { 1,4,5,2,6 }
    int key ==  5;
    return 0;
}